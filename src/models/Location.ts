export type LatitudeType = {
  maxLat: number | undefined;
  minLat: number | undefined;
};

export type LongitudeType = {
  maxLng: number | undefined;
  minLng: number | undefined;
};

export type LocationType = {
  lat: number;
  lng: number;
};
